// LET OP: de design-tokens van Kompal staan niet meer hier, maar als CSS-
// variabelen in `src/index.css` (--bg, --surface, --text, --accent, --radius, …).
// Zo werken licht en donker met één schakelaar en hoeft een kleurwijziging maar
// op één plek te gebeuren. Componenten gebruiken `var(--naam)` of de klassen
// (.kaart, .knop, .lijst, .bedrag, …) uit index.css.
//
// Dit bestand bestaat enkel nog als wegwijzer; er is niets meer te importeren.

export {}
